# Android versionCode floor for Google Play (monotonic). The effective versionCode is
# max(computed from git tag distance, versionCodeFloor). Bump before each store upload
# when you must exceed the last published versionCode (e.g. same-tag rebuild).
versionCodeFloor=0
